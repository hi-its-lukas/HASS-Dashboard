# HA Dashboard - Environment Variables
# ===========================================
#
# PRODUCTION (Docker): Copy to .env
# DEVELOPMENT (npm run dev): Copy to .env.local
#
# Note: User-specific settings (HA URL, entity mappings) are stored
# in the database and configured via Settings UI - NOT in env files.

# ===========================================
# REQUIRED - Server Secrets
# ===========================================

# Base URL where the dashboard is accessible (no trailing slash)
# Examples:
#   - Local only: http://192.168.1.100:5000
#   - With domain: https://dashboard.yourdomain.com
APP_BASE_URL=https://dashboard.yourdomain.com

# 32-byte hex key for encrypting OAuth tokens in database
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# ===========================================
# DATABASE
# ===========================================

# SQLite database path
# Docker uses: file:/data/ha-dashboard.db
# Local dev uses: file:./data/ha-dashboard.db
DATABASE_URL=file:./data/ha-dashboard.db

# ===========================================
# CADDY / HTTPS (production only)
# ===========================================

# Your domain name (without https://)
DOMAIN=dashboard.yourdomain.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=your@email.com

# ===========================================
# DEVELOPMENT OPTIONS
# ===========================================

# Set to 'true' to use mock data instead of real Home Assistant
# Only for development/testing without a HA instance
NEXT_PUBLIC_USE_MOCK=false
