services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hass-dashboard
    restart: unless-stopped
    ports:
      - "80:80"
      - "6000:6000"
    environment:
      - NODE_ENV=production
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SQLITE_URL=file:/data/hass-dashboard.db
      - HOSTNAME=0.0.0.0
    volumes:
      - ./data:/data
