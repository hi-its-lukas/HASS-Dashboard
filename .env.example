# HA Dashboard - Environment Variables
# ===========================================
#
# ZERO CONFIGURATION REQUIRED!
# The dashboard auto-generates encryption keys and detects the URL automatically.
#
# Only create a .env file if you need to override defaults.

# ===========================================
# OPTIONAL OVERRIDES
# ===========================================

# Override the detected URL (usually not needed)
# APP_BASE_URL=https://dashboard.yourdomain.com

# Use your own encryption key (auto-generated if not set)
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=your-64-character-hex-key

# ===========================================
# CADDY HTTPS (production with domain)
# ===========================================

# Your domain name (without https://)
# DOMAIN=dashboard.yourdomain.com

# Email for Let's Encrypt certificate notifications
# ACME_EMAIL=your@email.com

# ===========================================
# DEVELOPMENT
# ===========================================

# Use mock data instead of real Home Assistant
# NEXT_PUBLIC_USE_MOCK=true
