services:
  ha-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ha-dashboard
    restart: unless-stopped
    ports:
      - "5000:3000"
    environment:
      - NODE_ENV=production
      - APP_BASE_URL=${APP_BASE_URL:-http://localhost:5000}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      - DATABASE_URL=file:/data/ha-dashboard.db
    volumes:
      - ./data:/data
    networks:
      - ha-network

  caddy:
    image: caddy:2-alpine
    container_name: ha-dashboard-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN:-localhost}
      - EMAIL=${ACME_EMAIL:-}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - ha-network
    profiles:
      - https

networks:
  ha-network:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
